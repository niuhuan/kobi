 import 'package:flutter/material.dart';

class LocalCollectScreen extends StatelessWidget {
  const LocalCollectScreen({super.key});

   @override
   Widget build(BuildContext context) {
     return Scaffold(
       appBar: AppBar(
         title: Text('Local Collect'),
       ),
       body: Center(
         child: Text('Local Collect'),
       ),
     );
   }
 }
